import 'package:flutter/material.dart';

class PostFormScreen extends StatelessWidget {
  static String get createRouteName => 'postForm';
  const PostFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
